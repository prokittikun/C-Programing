#include <stdio.h>
#include <stdlib.h>
// linkedlist
struct Node__ {
    int value;
    struct Node__* next;
};

typedef struct Node__ Node;
typedef struct Node* NodePtr;

void printList(NodePtr start){
    
}
int main(){
    NodePtr starPtr; // point to the start of linkedlist
    starPtr = NULL;
}
