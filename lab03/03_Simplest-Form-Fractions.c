#include <stdio.h>
#include <stdlib.h>

int main()
{
    char x1[10], y1[10];
    fgets(x1, 10, stdin);
    fgets(y1, 10, stdin);
    int x = atoi(x1);
    int y = atoi(y1);

    while (1)
    {
        // todo
    }
}